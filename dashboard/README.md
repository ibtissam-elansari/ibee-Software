# Dashboard (placeholder)

Ce dossier est un placeholder pour un dashboard React.

Option rapide (recommandée):

```bash
cd dashboard
npm create vite@latest . -- --template react-ts
npm i
npm run dev
```

Ensuite, on connectera l'UI à l'API `http://localhost:8000`.

